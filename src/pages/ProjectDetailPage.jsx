import PageTitle from '../components/ui/PageTitle';
import StatusBadge from '../components/ui/StatusBadge';
import EmptyState from '../components/ui/EmptyState';
import { mockProjects } from '../data/mockProjects';
import { mockClients } from '../data/mockClients';
import { mockProjectTeam } from '../data/mockProjectTeam';
import { mockProjectWorkstreams } from '../data/mockProjectWorkstreams';
import { navigateTo } from '../utils/navigation';
const variant=(v)=>['Crítico','Bloqueado'].includes(v)?'critico':v==='Atenção'?'atencao':'sucesso';
export default function ProjectDetailPage({ projectId }){const project=mockProjects.find(p=>p.id===projectId); if(!project) return <EmptyState title="Projeto não encontrado" description="Volte ao portfólio para selecionar um projeto válido."><button onClick={()=>navigateTo('/portfolio')} className="rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-blue-700">Voltar ao Portfólio</button></EmptyState>; const client=mockClients.find(c=>c.id===project.clientId); const team=mockProjectTeam.filter(t=>t.projectId===projectId); const workstreams=mockProjectWorkstreams.filter(w=>w.projectId===projectId); return <div className="space-y-6"><PageTitle title={project.name} subtitle={project.description}/><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-blue-600">{project.code}</p><h2 className="mt-1 text-xl font-semibold text-slate-900">{project.clientName}</h2><p className="mt-2 text-sm text-slate-600">Cliente: {client?.name || project.clientName} • Segmento: {client?.segment || 'Não informado'}</p></div><div className="flex flex-wrap gap-2"><StatusBadge variant={variant(project.healthStatus)}>{project.healthStatus}</StatusBadge><StatusBadge variant="info">Gate {project.currentGate}</StatusBadge><StatusBadge variant="neutro">{project.agfLevel}</StatusBadge></div></div><dl className="mt-6 grid grid-cols-1 gap-4 md:grid-cols-3"><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">Gerente</dt><dd className="mt-1 text-sm font-semibold text-slate-800">{project.projectManager}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">Sponsor</dt><dd className="mt-1 text-sm font-semibold text-slate-800">{project.sponsor}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">Go Live previsto</dt><dd className="mt-1 text-sm font-semibold text-slate-800">{project.plannedGoLive}</dd></div></dl><div className="mt-6 flex flex-wrap gap-3"><button onClick={()=>navigateTo(`/projetos/${project.id}/agf`)} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Acessar Jornada AGF</button><button onClick={()=>navigateTo(`/projetos/${project.id}/agf/gate/3`)} className="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Acessar Gate 3</button></div></section><section className="grid grid-cols-1 gap-6 lg:grid-cols-2"><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="text-lg font-semibold text-slate-900">Time principal</h3><div className="mt-4 space-y-3">{team.map(m=><div key={m.id} className="rounded-xl border border-slate-100 bg-slate-50 p-3"><p className="text-sm font-semibold text-slate-800">{m.name} — {m.role}</p><p className="text-xs text-slate-500">{m.workstream} • {m.responsibility}</p></div>)}</div></article><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="text-lg font-semibold text-slate-900">Frentes do projeto</h3><div className="mt-4 space-y-3">{workstreams.map(w=><div key={w.id} className="rounded-xl border border-slate-100 bg-slate-50 p-3"><div className="flex justify-between gap-3"><p className="text-sm font-semibold text-slate-800">{w.name}</p><StatusBadge variant={variant(w.healthStatus)}>{w.healthStatus}</StatusBadge></div><p className="mt-1 text-xs text-slate-500">{w.mainDependency}</p></div>)}</div></article></section></div>}
