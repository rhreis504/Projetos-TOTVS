import StatusBadge from '../ui/StatusBadge';
import { statusVariant } from './gate3Ui';
const Item=({label,value})=><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">{label}</dt><dd className="mt-1 text-sm font-semibold text-slate-800">{value}</dd></div>;
export default function ProjectContextHeader({ project, healthStatus }){return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-blue-600">Contexto do projeto</p><h2 className="mt-1 text-2xl font-bold tracking-tight text-slate-900">{project.name}</h2><p className="mt-2 max-w-3xl text-sm leading-relaxed text-slate-600">{project.description}</p></div><div className="flex flex-wrap gap-2"><StatusBadge variant={statusVariant(project.status)}>{project.status}</StatusBadge><StatusBadge variant={statusVariant(healthStatus)}>{healthStatus}</StatusBadge><StatusBadge variant="info">Gate {project.currentGate}</StatusBadge></div></div><dl className="mt-6 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4"><Item label="Código" value={project.code}/><Item label="Cliente" value={project.clientName}/><Item label="Gerente do Projeto" value={project.projectManager}/><Item label="Sponsor" value={project.sponsor}/><Item label="Nível AGF" value={project.agfLevel}/><Item label="Origem" value={project.origin}/><Item label="Go Live previsto" value={project.plannedGoLive}/><Item label="Última atualização" value={project.lastUpdate}/></dl></section>}
