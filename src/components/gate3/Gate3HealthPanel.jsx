import StatusBadge from '../ui/StatusBadge'; import { statusVariant } from './gate3Ui';
export default function Gate3HealthPanel({ health, data }){const metrics=[['Pendências vencidas',data.pendingItems.filter(i=>i.status==='Vencida').length],['Riscos críticos',data.risks.filter(i=>i.severity==='Crítica').length],['Gaps sem decisão',data.gaps.filter(i=>i.requiredDecision&&i.status!=='Resolvido').length],['Mudanças abertas',data.changeRequests.filter(i=>['Solicitada','Em análise'].includes(i.status)).length],['Decisões vencidas',data.decisions.filter(i=>i.status==='Vencida').length],['Dependências bloqueadas',data.dependencies.filter(i=>i.status==='Bloqueada').length],['Entregáveis atrasados',data.deliverables.filter(i=>i.status==='Atrasado').length]];return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="text-lg font-semibold text-slate-900">Saúde do Projeto</h3><StatusBadge variant={statusVariant(health.status)}>{health.status}</StatusBadge></div><ul className="mt-4 list-disc space-y-1 pl-5 text-sm text-slate-600">{health.factors.map(f=><li key={f}>{f}</li>)}</ul><div className="mt-6 grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([label,value])=><div key={label} className="rounded-xl border border-slate-100 bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold text-slate-900">{value}</p></div>)}</div></section>}
