import StatusBadge from '../ui/StatusBadge';
import { statusVariant } from './gate3Ui';
export default function Gate3KpiCards({ project, data }){const cards=[['% de evolução do projeto',`${project.progress}%`,'info'],['Pendências abertas',data.pendingItems.filter(i=>['Aberta','Em andamento'].includes(i.status)).length,'atencao'],['Pendências vencidas',data.pendingItems.filter(i=>i.status==='Vencida').length,'critico'],['Riscos ativos',data.risks.filter(i=>['Aberto','Monitorando','Materializado'].includes(i.status)).length,'atencao'],['Riscos críticos',data.risks.filter(i=>i.severity==='Crítica').length,'critico'],['Gaps em aberto',data.gaps.filter(i=>!['Resolvido'].includes(i.status)).length,'atencao'],['Mudanças em análise',data.changeRequests.filter(i=>i.status==='Em análise').length,'atencao'],['Entregáveis para validação',data.deliverables.filter(i=>i.status==='Pronto para validação').length,'info'],['Decisões pendentes',data.decisions.filter(i=>['Pendente','Vencida'].includes(i.status)).length,'critico'],['Dependências bloqueadas',data.dependencies.filter(i=>i.status==='Bloqueada').length,'critico']];return <section className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-5">{cards.map(([label,value,variant])=><article key={label} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">{label}</p><div className="mt-3 flex items-end justify-between gap-3"><strong className="text-2xl font-bold text-slate-900">{value}</strong><StatusBadge variant={statusVariant(variant==='critico'?'Crítico':variant==='atencao'?'Atenção':'Em andamento')}>{variant==='critico'?'Alerta':variant==='atencao'?'Monitorar':'Info'}</StatusBadge></div></article>)}</section>}
