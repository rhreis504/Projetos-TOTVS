import StatusBadge from '../ui/StatusBadge';
import { statusVariant } from './gate3Ui';
export default function Gate3Overview({ project, workstreams, data, health }){const blocked=[...data.activities.filter(i=>i.status==='Bloqueada'),...data.dependencies.filter(i=>i.status==='Bloqueada')];const critical=[...data.pendingItems.filter(i=>i.status==='Vencida'||i.priority==='Crítica'),...data.risks.filter(i=>i.severity==='Crítica'),...data.issues.filter(i=>i.status==='Crítica')];return <div className="grid grid-cols-1 gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="text-lg font-semibold text-slate-900">Resumo operacional</h3><p className="mt-3 text-sm leading-relaxed text-slate-600">{project.name} está em execução controlada com {workstreams.length} frentes, {data.activities.length} atividades, {data.pendingItems.length} pendências e {data.deliverables.length} entregáveis monitorados para alimentar validações futuras.</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{workstreams.map(ws=><div key={ws.id} className="rounded-xl border border-slate-100 bg-slate-50 p-3"><div className="flex items-start justify-between gap-2"><p className="text-sm font-semibold text-slate-800">{ws.name}</p><StatusBadge variant={statusVariant(ws.healthStatus)}>{ws.healthStatus}</StatusBadge></div><p className="mt-2 text-xs text-slate-500">{ws.mainDependency}</p></div>)}</div></section><section className="space-y-4"><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="text-base font-semibold text-slate-900">Alertas críticos</h3><ul className="mt-3 space-y-2 text-sm text-slate-700">{critical.map(item=><li key={item.id} className="rounded-lg border border-red-200 bg-red-50 px-3 py-2">{item.code || item.title}: {item.description}</li>)}</ul></div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="text-base font-semibold text-slate-900">Saúde simulada: <span className="text-blue-700">{health.status}</span></h3><ul className="mt-3 list-disc space-y-1 pl-5 text-sm text-slate-600">{health.factors.map(f=><li key={f}>{f}</li>)}</ul></div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="text-base font-semibold text-slate-900">Itens bloqueados e próximos marcos</h3><p className="mt-2 text-sm text-slate-600">{blocked.length ? `${blocked.length} item(ns) bloqueado(s) exigem atuação executiva.` : 'Sem bloqueios operacionais nos dados simulados.'}</p></div></section></div>}
