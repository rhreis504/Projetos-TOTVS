import { ArrowRight } from 'lucide-react';
import { navigateTo } from '../../utils/navigation';
export default function GateCard({ gate }) { return <article className="bg-white rounded-2xl border border-slate-200 shadow-sm p-6 flex flex-col"><div className="flex items-center justify-between mb-4"><span className="text-xs font-bold tracking-wider uppercase text-blue-600">Gate {gate.id}</span><span className="text-xs text-slate-500">{gate.steps.length} etapas</span></div><h3 className="text-base font-semibold text-slate-900 mb-2">{gate.name}</h3><p className="text-sm text-slate-600 leading-relaxed flex-1">{gate.objective}</p><button onClick={() => navigateTo(`/agf/gate/${gate.id}`)} className="mt-5 inline-flex items-center justify-center gap-2 rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Acessar Gate <ArrowRight className="w-4 h-4" /></button></article>; }
