import StatusBadge from '../ui/StatusBadge';
const owners = ['PMO TOTVS', 'Cliente', 'Parceiro', 'Sponsor'];
export default function GateStepList({ steps }) { return <div className="bg-white rounded-2xl border border-slate-200 shadow-sm divide-y divide-slate-100">{steps.map((step,index)=><div key={step} className="p-4 flex flex-col md:flex-row md:items-center gap-3 justify-between"><div><p className="text-sm font-semibold text-slate-800">{index+1}. {step}</p><p className="text-xs text-slate-500 mt-1">Responsável: {owners[index % owners.length]}</p></div><div className="flex items-center gap-3"><StatusBadge variant={index % 4 === 0 ? 'sucesso' : index % 3 === 0 ? 'atencao' : 'info'}>{index % 4 === 0 ? 'Concluída' : index % 3 === 0 ? 'Atenção' : 'Em andamento'}</StatusBadge><button className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Abrir</button></div></div>)}</div>; }
